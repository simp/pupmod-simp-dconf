---
fixtures:
  repositories:
    # Only needed so spec tests can resolve `compliance_engine::enforcement`
    # for the `simp:defaults` profile. NOT a module dependency (see metadata.json).
    compliance_engine:
      repo: https://github.com/simp/rubygem-simp-compliance_engine.git
      ref: '1.0.0'
    concat:  https://github.com/simp/puppetlabs-concat.git
    inifile: https://github.com/simp/puppetlabs-inifile.git
    stdlib:  https://github.com/simp/puppetlabs-stdlib.git
